import React from 'react';
import { HomePage } from './HomePage';

// ProductsPage can be the same as HomePage or have additional filters
export const ProductsPage = () => {
  return <HomePage />;
};
